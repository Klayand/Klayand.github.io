# Zikai Zhou

Personal academic homepage for [Klayand.github.io](https://klayand.github.io/).

This site is intentionally static:

- `index.html` contains the page content.
- `styles.css` contains the visual system.
- `images/` contains profile and favicon assets.

GitHub Pages can serve this repository directly from the main branch.
